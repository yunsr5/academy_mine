<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ac.kr.academy.mapper.notice.NoticeMapper">
    <insert id="insertNotice" parameterType="com.ac.kr.academy.domain.notice.Notice">
        <selectKey keyProperty="id" order="BEFORE" resultType="long">
            SELECT notice_seq.NEXTVAL FROM dual
        </selectKey>
        INSERT INTO notice (
            id, title, content, start_date, end_date, created_at,
            deadline, view_count, is_urgent
        ) VALUES (
            #{id}, #{title}, #{content}, #{startDate}, #{endDate}, SYSDATE,
            #{deadline}, 0, 0
        )
    </insert>

    <select id="getAllNotices" resultType="com.ac.kr.academy.domain.notice.Notice" >
        SELECT id, title, start_date, end_date, created_at, view_count, is_urgent
        FROM notice
        ORDER BY is_urgent DESC, created_at DESC
    </select>
    
    <select id="findByNoticeId" parameterType="long" resultType="com.ac.kr.academy.domain.notice.Notice">
        SELECT * FROM notice
        WHERE id = #{id}
    </select>

    <update id="updateNotice">
        UPDATE notice
        SET title = #{title},
            content = #{content},
            start_date = #{startDate},
            end_date = #{endDate},
            deadline = #{deadline},
            is_urgent = #{isUrgent}
        WHERE id = #{id}
    </update>

    <delete id="deleteNotice" parameterType="long">
        DELETE FROM notice
        WHERE id = #{id}
    </delete>

    <update id="increaseViewCount">
        UPDATE notice
        SET view_count = view_count +1
        WHERE id = #{id}
    </update>

</mapper>